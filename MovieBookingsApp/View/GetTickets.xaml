<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MovieBookingsApp.GetTickets"
             xmlns:namespace="clr-namespace:MovieBookingsApp"
             x:DataType="namespace:GetTickets_ViewModel"
             Title="{Binding Title}">
    <CollectionView
            ItemsSource="{Binding Movies}"
            SelectionMode="Single"
            SelectionChangedCommand="{Binding SelectMovieCommand}" 
            SelectedItem="{Binding SelectedMovie}">
        <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical"
                Span="2" />
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="namespace:Movie">
                <VerticalStackLayout
                    Padding="15">
                    <Image Aspect="AspectFill"
                            HeightRequest="260"
                            HorizontalOptions="Center"
                            VerticalOptions="Center"
                            Source="{Binding ImageURI}"/>
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>